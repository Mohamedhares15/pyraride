const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { try { await prisma.$executeRawUnsafe(`ALTER TABLE "Horse" ADD COLUMN "color" TEXT;`); console.log("Added color to Horse"); } catch(e) { console.log("Error or already exists:", e.message); } try { await prisma.$executeRawUnsafe(`ALTER TABLE "HorseChangeRequest" ADD COLUMN "proposedColor" TEXT;`); console.log("Added proposedColor"); } catch(e) { console.log("Error or already exists:", e.message); } } main().catch(console.error).finally(()=>prisma.$disconnect());
